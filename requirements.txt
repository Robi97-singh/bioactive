# Project Bioactive — Requirements
# Generated: June 2026
# Python 3.10.12

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=2.5.1                        # PyTorch (install with CUDA separately)
torchvision>=0.20.1                 # Vision models and transforms

# ── Vision Models ─────────────────────────────────────────────────────────────
transformers>=5.9.0                 # DINOv2, ViT-B/16 via HuggingFace
timm>=1.0.27                        # ConvNeXt, ViT-B/16 via timm
open-clip-torch>=2.24.0             # BiomedCLIP, CLIP ViT-L/14
huggingface-hub>=0.24.0             # HuggingFace model downloading

# ── Chemistry / Bioinformatics ────────────────────────────────────────────────
rdkit>=2023.9.1                     # Molecular fingerprints, InChI conversion
# Note: install via: pip install rdkit

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.0.0                       # DataFrame operations
numpy>=1.24.0                       # Numerical operations
scipy>=1.11.0                       # Statistical functions
scikit-learn>=1.3.0                 # Metrics, clustering utilities

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=10.0.0                      # PNG read/write
tifffile>=2023.7.10                 # 16-bit TIFF reading
imagecodecs>=2023.7.10              # LZW TIFF decompression (required for some sources)
albumentations==1.3.1               # Image augmentations (PINNED — newer versions break API)

# ── AWS / S3 ──────────────────────────────────────────────────────────────────
boto3>=1.28.0                       # S3 downloads (JUMP-CP images)
awscli>=1.29.0                      # AWS CLI (optional, for manual S3 browsing)

# ── Experiment Tracking ───────────────────────────────────────────────────────
wandb>=0.15.0                       # Weights & Biases (set WANDB_MODE=disabled to skip)

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0                        # Progress bars
joblib>=1.3.0                       # Parallel processing
requests>=2.31.0                    # HTTP requests (ChEMBL REST API fallback)
h5py>=3.9.0                         # HDF5 file support

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0                   # Plotting (training curves, ROC curves)
seaborn>=0.12.0                     # Statistical visualisation

# ── Dimensionality Reduction ──────────────────────────────────────────────────
umap-learn>=0.5.3                   # UMAP embeddings (optional, for visualisation)

# ──────────────────────────────────────────────────────────────────────────────
# INSTALLATION NOTES
# ──────────────────────────────────────────────────────────────────────────────
#
# 1. Install PyTorch with CUDA FIRST (before this file):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 2. Then install remaining requirements:
#    pip install -r requirements.txt
#
# 3. albumentations MUST be pinned to 1.3.1:
#    pip install albumentations==1.3.1 --force-reinstall
#
# 4. Set environment variables before training:
#    export WANDB_MODE=disabled
#    export PYTHONWARNINGS="ignore::FutureWarning"
#    export HF_HOME=/mnt/ssd8/bioactive/model_cache
#
# 5. For cloud instances, mount data SSD and set paths in params/*.json
#
# ──────────────────────────────────────────────────────────────────────────────